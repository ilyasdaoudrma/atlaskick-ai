# Optional: personal TheSportsDB API key (https://www.thesportsdb.com).
# The app falls back to the free community key '123' if unset — fine for
# development, but rate-limited. Never commit a real key.
VITE_SPORTSDB_KEY=
